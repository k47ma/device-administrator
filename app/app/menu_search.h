// This is the interface for searching menu module
#include "read.h"

// search_menu(list) provides a series of operations on searching for information
// effects: allocates memory, frees memory before returning
// notes: no information will be stored in this menu
void search_menu(const struct llist *list);
